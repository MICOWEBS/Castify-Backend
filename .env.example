APP_NAME=Netflix
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
FRONTEND_URL=http://localhost:8080

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=netflix
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_DRIVER=pusher
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=resend
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="support@netflixclone.com"
MAIL_FROM_NAME="${APP_NAME}"

# Resend configuration
RESEND_API_KEY=your_resend_api_key_here

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# Scout Search Configuration
SCOUT_DRIVER=database

# DRM Configuration
DRM_ENABLED=false
DRM_PROVIDER=widevine
DRM_LICENSE_SERVER=
DRM_CONTENT_KEY=
DRM_API_KEY=
DRM_SIGNING_KEY=
DRM_SIGNING_IV=
DRM_CERTIFICATE_PATH=

# Speech to Text Configuration
SPEECH_TO_TEXT_ENABLED=false
SPEECH_TO_TEXT_PROVIDER=google
SPEECH_TO_TEXT_API_KEY=
SPEECH_TO_TEXT_ADDITIONAL_LANGUAGES=es,fr

# Google Cloud Speech-to-Text
GOOGLE_CLOUD_PROJECT_ID=
GOOGLE_APPLICATION_CREDENTIALS=

# AWS Transcribe (can reuse AWS credentials above)

# Azure Speech Service
AZURE_SPEECH_REGION=eastus
AZURE_SPEECH_KEY=

# FFmpeg Configuration
FFMPEG_BINARY_PATH=/usr/bin/ffmpeg
FFPROBE_BINARY_PATH=/usr/bin/ffprobe
FFMPEG_TIMEOUT=3600
FFMPEG_THREADS=2

# Queue Configuration
QUEUE_VIDEO_MAX_ATTEMPTS=3
QUEUE_VIDEO_RETRY_AFTER=600
QUEUE_VIDEO_BLOCK_FOR=5
QUEUE_VIDEO_MAX_JOBS=5

# Notifications
ADMIN_NOTIFICATION_EMAILS=admin@example.com,tech@example.com
NOTIFICATION_SLACK_WEBHOOK=
ERROR_NOTIFICATION_THRESHOLD=5
PROCESSING_ALERT_THRESHOLD=30
